// 사이트 전역 설정. 이름·도메인 바꿀 땐 여기만 고치면 전부 반영된다.
export const SITE = {
  name: 'K-Snack Basak',
  url: 'https://snackdex.pages.dev', // 도메인 사면 여기 교체
  defaultLang: 'en',
  languages: ['en', 'ja'], // 나중에 'zh','id' 추가만 하면 라우트가 자동 생성된다
};
